mode: ContinuousDelivery

next-version: 1.0.0

branches:
  main:
    regex: ^main$
    increment: Patch
    label: ''
    is-main-branch: true

  dev:
    regex: ^dev(elop)?$
    increment: Minor
    label: beta
    source-branches:
      - feature
      - release

  release:
    regex: ^release[/-]
    increment: Patch
    label: rc
    is-release-branch: true
    source-branches:
      - dev

  feature:
    regex: ^feat[/-]|^feature[/-]
    increment: Minor
    label: alpha
    source-branches:
      - dev